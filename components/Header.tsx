import { FunctionComponent } from "react";

const Header: FunctionComponent = () => {
  return (
    <header className="sticky z-9999 top-0 w-full px-8 h-16">
      <div className="flex items-center h-full">this is the Header</div>
    </header>
  );
};

export default Header;
